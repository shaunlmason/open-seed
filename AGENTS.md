# AGENTS.md

**This repository is retired and is not worked in.** See
[`README.md`](README.md).

Coordination moved to **Seed**, which lives at
[shaunlmason/open-seed-v2](https://github.com/shaunlmason/open-seed-v2). Work
happens there, under that repository's own `AGENTS.md`.

There is no task queue here, no loop, and no gate. The v1 shim
(`scripts/seed`), its contract (`.seed/`) and the workflows that drove them are
gone; the `seed-state` ref they wrote is frozen at its final anchor and kept
read-only for its history.

If you were sent here to do work, you are in the wrong repository.

## What this repository is for now

Reading. It holds the records the retirement kept: `decisions/`, `plans/`,
`receipts/`, `memory/`, and the research corpus under `docs/research/` that the
successor's charter cites as its lineage.

Treat everything here as history. Nothing in it describes how the successor
works: for that, read the successor's `spec/` and `AGENTS.md`.
