---
id: os-a00d3f34
title: 'next: the contention benchmark at target scale — a scheduled run with hundreds of writers through the hook, tracked against the perf budgets (III.C row 4)'
state: blocked
priority: P2
squad: core
blocked_on:
    - plan:293
created_at: "2026-09-03T22:57:42Z"
updated_at: "2026-09-03T23:14:53Z"
---

Build plan section 3 and the Phase 12 exit record route charter III.C row 4 here: the per-PR storm stays at 24 writers under the perf gate (next/perf/budgets.json, plans/os-7508ab9e.md D6), and the target scale (hundreds of concurrent actors, no lost updates) is a scheduled run, because a storm at that scale costs minutes a run. The conformance table (next/spec/conformance.json, os-83bc3d84) carries C.4 as partial with this run named.

Expected shape, for the plan to settle: a second budget profile (next/perf/budgets-scale.json: writers in the hundreds, the history sized to match, ceilings with measured provenance) read by the same perfgate, so the scaled run and the per-PR gate are one measurement at two sizes rather than two tools; a scheduled workflow (weekly plus workflow_dispatch) running it on a runner, attaching the last reading as an artifact and failing when a metric misses its ceiling after the one cold re-measure; the no-lost-updates assertion at that scale (every writer's admitted record present exactly once in the folded chain). Bounds: the per-PR gate and its budgets unchanged; the workflow file lives under .github/workflows, a protected path that lands only through owner review at merge (the guardrails' PR plus owner-review rule), the one v1 surface beside the docs tree the card touches. The Phase 13 exit record flips C.4 to met citing the scheduled run's first green readings. Tier: standard. Deps: none (Phase 13's items are on main).
