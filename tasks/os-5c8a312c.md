---
id: os-5c8a312c
title: 'next: Phase 12 item 2 — forge-hosted admission service (stateless, sole-writer) and the protections desired-state reconciler'
state: in_progress
priority: P1
squad: core
claim:
    actor: seed-next-implementer
    token: c-c9a9b9b3bd5f9dab
    claimed_at: "2026-09-03T00:52:27Z"
    lease_expires: "2026-09-03T01:52:27Z"
created_at: "2026-09-03T00:10:12Z"
updated_at: "2026-09-03T00:52:27Z"
---

Build plan Phase 12 item 2 (docs/next-build-plan.md): the forge-hosted admission service (stateless, sole-writer pattern) and the protections desired-state reconciler.

Charter: §II.2's second enforced posture — where server hooks are unavailable, the ledger ref is written only by a dedicated admission identity, a small service or endpoint that actors PROPOSE to, and forge protections make that identity the ref's sole writer; the service holds no durable state. §II.14: forge-side protections as declared desired-state, reconciled by command — branch protection with required checks, ledger ref writable only by admission, immutable release tags, least-privilege scheduled/CI identities, no scheduled job pushing to the default branch. Conformance: III.B rows 1 and 3 (all three postures implemented; the validator the ref's sole writer), III.L row 5 (protections declared and reconciled), III.P row 2 (the validator ships in hook AND service form, both stateless, both rebuildable from a clone).

What the tree has: next/cmd/seed-admit is the hook form (Phase 2.3, #94), importing the one rule set in internal/admit; its drills (#99) install it on a bare remote and kill-and-replace the host. internal/posture names enforced-forge-hosted and carries ForgeHostedGap, the honest sentence that this service does not exist yet; seed doctor prints it. v1's `scripts/seed init-github` prints a protections checklist and says the engine cannot call the API.

Expected shape, for the plan to settle: the service form of seed-admit — a proposal endpoint that takes signed events, runs the same admission the hook runs over a fresh clone, and pushes the admitted stream to the ledger ref under the admission identity; refusals as the structured envelope; no state but the clone (kill-and-replace drilled as for the hook). The protections file as declared desired-state (consumed later by item 4's preseed), a reconciler verb that plans and applies it through a forge adapter (GitHub first, with a dry-run/plan mode CI can run credential-free), and the doctor reporting posture, service and protections drift. The shared adversary table from #99 must yield identical decisions under both enforced postures. Hosting and credentials are the deployment's; what lands is the artifact, the reconciler and the drills. Phase 12 opens when the Phase 11 exit record merges; plan now, implement as a draft until then (decisions/0003).
