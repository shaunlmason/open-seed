---
id: os-46f19906
title: 'next: tool-edge authority passes derived from admitted facts (backlog, §II.5 capability scoping, §II.9 executor adapters, §II.10 least standing capability)'
state: backlog
priority: P2
squad: core
labels:
    - next
created_at: "2026-09-06T11:46:02Z"
updated_at: "2026-09-07T04:20:21Z"
---

docs/next-build-plan.md §3 "Borrowed from surveyed tools (2026-09-06, pigeon)", the one card. Source: github.com/pigeonlabsHQ/pigeon (Pigeon Pass, a v0.1 delegated-authority credential: Ed25519-signed chain, every child provably narrower than its parent, closed constraint set with rate/count metered against every ancestor, fail-closed verifier). At wake, the executor adapter mints a short-lived pass for the run from admitted facts only: capabilities from the lane tool policy, resources from the contract routing (repository, branch, declared paths, environments), count from the budget reservation, expiry at the lease end, subject the run key. A tool proxy the adapter runs outside the agent process (container and cloud adapters; the local adapter declares it cannot) verifies before each side effect and refuses on denial; a harness that spawns subagents hands each an attenuated child. Three rules keep the ledger the one authority (charter §I.3, §II.18): the pass is a projection, rebuildable from the chain and never presented at admission, so actor.granted stays the only capability data admission reads; count is a derived cap on a reservation the ledger owns, never a second budget; denials are metered onto the observation stream as tool.refused (§II.9 telemetry, never an escalation) and joined to the receipt as evidence. Format: pigeon SPEC §4 to §9 in Go under next/internal/pass with its fixtures/ as the conformance corpus; the credential never enters the ledger, so the JCS default governs nothing it touches (one decision-log line). Mock-total like every adapter (§II.13). Not a dependency: the reference is Python, in-memory stores, own canonical form, and no proof the leaf subject holds its key, so the harness binds the pass. Not conformance-blocking; changes no Part III row.

## Comment cm-0138b462 (seed-next-implementer, 2026-09-07T04:20:21Z)

Card text revised after PR #362 review (four findings, all confirmed against the tree). The original body claimed four inputs Seed does not record: tool_policy is an opaque tuple string and a contract routing is one squad name (internal/transition); branch names are forge state the ledger cannot see (next/spec/verdicts.md); Seed holds no lease at all, a claim standing until deliberate exit or reap (next/spec/observations.md); and reservation units are abstract until Phase 7.3 metering (next/spec/budgets.md), so a reservation amount is not an operation count. The card now makes the mapping its first item: capabilities, resources and TTL come from a declared tool-policy map read as admission policy, not chain validity, the way the claim ceiling and routing rule read the declaration (next/spec/postures.md); the adapter re-mints per metering cycle rather than citing a deadline that does not exist; the operation cap is a declared number kept separate from the budget; and tool.refused denials surface in the packet and the report, never in a receipt, whose inputs are exclusively verifier-executed or verifier-read and reproducible from the submission head. Authoritative text: docs/next-build-plan.md §3, PR #362.
