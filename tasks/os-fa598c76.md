---
id: os-fa598c76
title: 'sync: prune orphaned files in the harness fan-outs (.claude/agents, .claude/skills, .agents/skills)'
state: backlog
priority: P3
squad: core
created_at: "2026-08-24T02:36:20Z"
---

os-221f5929 gave the plugin channel's owned roots (plugin/, .claude-plugin/) a pruning sweep: sync deletes generated files no source produces and reports them as drift until it does. The older harness fan-outs still have the original gap: delete .seed/agents/foo.md and .claude/agents/foo.md stays on disk with 'sync --check' clean, so a removed role or skill keeps being offered to the harness.

Not fixed in os-221f5929 because it is outside that plan's file scope, and because those roots each hold a hand-written README.md marker, so the 'delete anything the render did not produce' rule needs either an exemption list or the same treatment the plugin root got (render the marker, making the root 100% generated). The second is cleaner and matches what already shipped.

Scope: extend sync's owned-root sweep to .claude/agents/, .claude/skills/ and .agents/skills/; render their README markers; tests for a deleted role and a deleted skill in each fan-out.
