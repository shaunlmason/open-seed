---
id: os-2420b91e
title: 'next: Phase 11 — callscript as a design reference for the drafted-workflow authoring surface'
state: backlog
priority: P3
squad: core
created_at: "2026-09-01T02:45:02Z"
---

Design-reference card, not adoption. vercel-labs/callscript (Apache-2.0, TypeScript): the model authors a JS-looking script that is PARSED into an inert JSON plan (three verbs plus modifiers), statically validated against mounted tool schemas before anything runs, bounded fan-outs by construction, suspend/resume as a serializable record, no sandbox because nothing executes as code. Relevant when build plan Phase 11 item 5 lands (recurring-shape detection -> drafted workflow -> mock validation -> proposal PR): a model-friendly authoring frontend that compiles into the validated DAG definition beats asking a model to emit registry JSON directly, and callscript's pointed validator refusals (teach-the-spelling-on-retry; one retry converges) are the pattern to study, alongside its suspend-for-approval gate and search/describe tool discovery. Constraints that hold regardless: charter II.13/III.M stay DAG+gates (expressiveness creep stays rejected), the engine stays Go (reference only, never a dependency), and per-event admission is untouched (batched plans are a workflow-layer concern, never an admission-boundary one). Assessed 2026-09-01 in-session during Phase 8.
