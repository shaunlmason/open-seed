---
id: os-ad610334
title: 'next: Phase 13 item 3 — a non-primary forge adapter for the forge extras (III.N)'
state: blocked
priority: P2
squad: core
blocked_on:
    - plan:275
created_at: "2026-09-03T02:39:51Z"
updated_at: "2026-09-03T09:16:48Z"
---

Build plan Phase 13 item 3 (docs/next-build-plan.md): a non-primary forge adapter for the forge extras.

Charter: §II.15 (interoperability: the core loop runs on any git remote supporting the declared admission posture; forge extras — PR observation, protections, identities — are adapters), III.N row 2 ("The core loop runs on any git remote supporting the declared admission posture; at least one non-primary forge is supported by adapters"). Deps: Phase 12.

What the tree has: the two enforced postures over any git remote (next/spec/postures.md: the pre-receive hook and the proposal service, both from one rule set); the protections reconciler with a GitHub REST adapter and a snapshot adapter behind one interface (internal/protections; seed protections plan|apply); merge.observed as the observer's forge fact ({merged, pr}); the compromised-actor drill and the release gate against GitHub's protections (Phase 12 item 1). The forge extras are GitHub-shaped in exactly the places the adapters name.

Expected shape, for the plan to settle: a second forge adapter (GitLab or Gitea/Forgejo — the plan chooses, with a credential-free CI drill against the forge's API surface or a faithful fake) for protections (rulesets or their equivalent, required checks, the ledger ref's sole writer), for the observer's merge facts, and for the release gate; the declaration naming the forge; the drills that hold both adapters to one decision table; the handbook's per-forge page. Phase 13 opens when the Phase 12 exit record merges; plan now, implement as a draft until then (decisions/0003).
