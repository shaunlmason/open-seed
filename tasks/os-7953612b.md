---
id: os-7953612b
title: 'next: sharded admission intake (III.B row 6, MAY) — a true extra'
state: backlog
priority: P3
squad: core
created_at: "2026-09-03T22:57:47Z"
---

Charter III.B row 6: admission MAY shard proposal intake without changing semantics; ordering remains solely the admitted chain. Recorded not claimed at the Phase 12 exit; the conformance table (os-83bc3d84) carries B.6 as open with this backlog line named. Build plan section 3 files it as a true extra, not conformance-blocking.

Expected shape, for the plan to settle: intake shards as independent proposal queues in the forge-hosted admission service (seed-admit serve, plans/os-5c8a312c.md), each shard judging with the one rule set and proposing onto the one ledger ref through the same optimistic fast-forward loop, so the chain remains the only order and a shard adds throughput at the door without a second authority; a drill proving byte-identical folds and projections across shard counts, and the storm at two shard counts giving the same admitted set. Bounds: no change to the hook posture, the rule set or the envelope; the cooperative posture has no intake to shard. Tier: standard. Not conformance-blocking.
