---
id: os-222189a3
title: 'next: the flywheel engine drill''s skip path races TempDir cleanup on macOS (directory not empty)'
state: ready
priority: P3
squad: core
created_at: "2026-09-03T23:10:45Z"
updated_at: "2026-09-04T02:16:34Z"
---

Seen on #290's platform (macos-latest) leg, run 33815519050: TestValidationRunsThroughTheEngineFromAStagingWorktree (internal/flywheel/flywheel_test.go) skips by name when the pinned v1 engine is not in the bootstrap cache (requireEngine, D3 of plans/os-9075c308.md), and the skip then FAILS on the harness's own cleanup: testing.go TempDir RemoveAll: unlinkat .../.git/objects: directory not empty. A git process spawned by fixtureRepo or instantiate is still writing objects when t.TempDir removes the tree, so a skipped drill reports as failed. The same leg is green on main's last three pushes; the race is the skip path's, where the fixture is built and then abandoned at once. Expected shape: the fixture hardening os-c4e8b57a and os-711b3028 applied here (gc.auto 0, gc.autoDetach false on every fixture repo the drill creates, and the engine check moved before the fixture is built so a skip builds nothing). Tests only. Tier: trivial.
