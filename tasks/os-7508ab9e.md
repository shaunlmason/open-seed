---
id: os-7508ab9e
title: 'next: Phase 12 item 3 — checkpoint trust docs with the replay-equals-genesis CI proof; performance budgets tracked in CI'
state: in_progress
priority: P1
squad: core
claim:
    actor: seed-next-implementer
    token: c-fc9b528746445348
    claimed_at: "2026-09-03T01:11:46Z"
    lease_expires: "2026-09-03T03:35:31Z"
created_at: "2026-09-03T00:10:14Z"
updated_at: "2026-09-03T02:36:55Z"
---

Build plan Phase 12 item 3 (docs/next-build-plan.md): checkpoint trust docs with the replay-equals-genesis CI proof, and performance budgets (admission latency, replay time, projection rebuild time) tracked in CI.

Charter: III.A row 8 — checkpoints signed by maintenance/operator identities, embedding the projection state hash and referencing a retrievable canonical snapshot (landed, #205: `system.checkpoint` {format, snapshot, location, position}, seed.projection.v1, fetch → verify → start); what remains of the row is the trust half: a fresh clone's verification obligations (full replay once, or explicit trust in the signer set) DOCUMENTED and the choice DECLARED, not defaulted, and replay-from-checkpoint equals replay-from-genesis proven in CI. III.A row 12 — ledger performance budgeted and tracked in CI: admission latency, replay time, projection rebuild time, against a representative history. III.C row 4 (a contention benchmark at hundreds of concurrent actors, tracked in CI) is the neighbouring row with no owner; the plan decides whether it rides here or is carded on its own, and says why.

What the tree has: next/spec/maintenance.md "Checkpoints carry a snapshot a reader can start from"; internal/checkpoint; TestMaintainCheckpointIsStartableByAFreshReader (the round trip). No benchmark exists anywhere under next/ (grep finds none), and nothing declares a budget.

Expected shape, for the plan to settle: next/spec/checkpoints.md as the trust document (what a signed checkpoint proves and to whom; the signer set; the fresh clone's two obligations and the config field that declares the choice; the staleness bound the ledger spec already names); a reader path that starts from a checkpoint and the CI drill asserting its projections are byte-identical to a genesis replay at the cited position; a deterministic representative history generator (seeded, sized by declaration) and a benchmark harness whose thresholds live in a checked-in budget file, run under make check-next with variance handled honestly (a budget is a ceiling with stated headroom, not a flaky exact number); the numbers surfaced where an operator reads them. Phase 12 opens when the Phase 11 exit record merges; plan now, implement as a draft until then (decisions/0003).

## Evidence ev-15464e79 (pr, seed-next-implementer, 2026-09-03T02:36:55Z)

https://github.com/shaunlmason/open-seed/pull/253
