---
id: os-5781a026
title: 'next: require-approval per verb on the machine surface — approval requests resolved attributably in the operator''s inbox (III.L row 4''s third mode)'
state: ready
priority: P1
squad: core
created_at: "2026-09-04T22:20:25Z"
updated_at: "2026-09-04T22:22:24Z"
---

Charter II.14 defines per-verb policy on the machine-protocol surface as allow/deny/require-approval by actor and risk class, with approvals as request events resolved attributably in an operator inbox, and III.L row 4 asks that this policy govern the surface. Review on #320 (chatgpt-codex-connector) found the tree has allow and deny (the grant table, the declaration's ceiling and routing rules, drilled on serve by os-8ecef90f) and no require-approval mode: no declaration names a verb that needs approval, no request event asks for one, and nothing consumes an approval at admission. os-8ecef90f therefore leaves the row partial and routes its third mode here. This card adds it as additive catalog growth (no version bump): a guardrails.approvals block in the declaration naming, per verb, the actor kinds and the tier floor that need approval; three fact verbs, approval.requested (the act's actor or any lane asks, naming the verb and actor, on the contract), approval.granted and approval.denied (operator only, citing the request), so the approval is a signed record resolved by a human; a policy rule in admission that refuses a governed act by a matching kind on a contract at or above the floor unless an open granted approval for that subject, verb and actor stands, naming the request to file, and consumes it on the admitted act; the obligations projection carrying approval.pending owed by the operator lane so the request lands in the inbox the situation read is; loop verbs seed approval request|grant|deny deriving every argument they can; the affordance catalog drafting the three where they are legal; the machine-surface drill firing the whole path through serve; III.L row 4 flipped to met on allow, deny and require-approval together. Tier: standard. Plan-first.
