---
id: os-083112ac
title: 'next: Phase 13 item 2 — the remaining executor adapters: container, cloud agent session, enrolled remote worker (III.H)'
state: in_progress
priority: P2
squad: core
claim:
    actor: seed-next-implementer
    token: c-4941db5f5c1ae587
    claimed_at: "2026-09-03T09:11:40Z"
    lease_expires: "2026-09-03T10:11:40Z"
created_at: "2026-09-03T02:39:49Z"
updated_at: "2026-09-03T09:11:40Z"
---

Build plan Phase 13 item 2 (docs/next-build-plan.md): the remaining executor adapters — container, cloud agent session, enrolled remote worker.

Charter: §II.9 (the supervisor: executor adapters implement provision/wake/meter and report the provisioned runtime tuple; the adapter interface is public), III.H row 6 ("Executor adapters implement provision/wake/meter, report the provisioned runtime tuple, and exist for at least: local worktree, container, cloud session, enrolled remote worker; the adapter interface is public"), III.H row 4 (where a substrate cannot stop spend synchronously, the budget is a documented risk limit per adapter). Deps: Phase 12.

What the tree has: next/spec/executors.md — the adapter contract, the spend bracket, the local worktree adapter, disposal and the loss window, preemption graceful-first with force as the drilled fallback; run.started declaring the runtime tuple; metering on the observation channel settling at run end; the disposability drill with randomized kills. One adapter (local worktree) exists.

Expected shape, for the plan to settle: the three adapters against the same public interface (provision, wake, meter, dispose, the tuple they report), each with its budget posture stated honestly (which can stop spend synchronously, which is a risk limit), the container adapter runnable in CI credential-free (a local OCI runtime or a documented fake with the same contract drilled), the cloud session and enrolled remote worker adapters with their credential and enrollment paths declared in seed.json and drilled against fakes where CI cannot hold credentials; the disposability and preemption drills run per adapter; the report naming the adapter per run. Phase 13 opens when the Phase 12 exit record merges; plan now, implement as a draft until then (decisions/0003).
