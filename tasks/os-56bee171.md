---
id: os-56bee171
title: 'next: Phase 13 item 1 — racing mode as the per-squad opt-in with first-verified settlement (III.F)'
state: in_progress
priority: P2
squad: core
claim:
    actor: seed-next-implementer
    token: c-94468ebbc790f69a
    claimed_at: "2026-09-03T04:10:58Z"
    lease_expires: "2026-09-03T05:10:58Z"
created_at: "2026-09-03T02:39:46Z"
updated_at: "2026-09-03T04:10:58Z"
---

Build plan Phase 13 item 1 (docs/next-build-plan.md): racing mode as the per-squad opt-in with first-verified settlement.

Charter: §II.6 (work contracts — claims are exclusive with fencing and settle at admission; racing mode, where more than one worker attempts the same contract and the first verified submission wins, exists only as an explicit per-squad opt-in with its compute cost stated), III.F row 7 ("Racing mode exists only as an explicit per-squad opt-in with its compute cost stated; offline claiming is impossible by construction"). Deps: Phase 12 (the exit record).

What the tree has: exclusive claim.taken with fences and the optimistic push loop (next/spec/lifecycle.md, modes.md, loop-verbs.md: a lost claim take is not an error in fleet mode); the preseed's per-squad guardrails (Phase 12 item 4: squads with default and max_agent tiers in seed.json); budget reservations fenced to the claim; the verdict chain with independence levels; offers scoped by eligibility (offers.md). No verb, table row or declaration field speaks of racing today, and the fleet drills assume one holder per contract.

Expected shape, for the plan to settle: the per-squad opt-in as a declaration field (next to max_agent), which the admission boundary reads so that a racing claim on an opted-in squad's contract admits beside the standing holder rather than refusing exclusivity, each racer with its own fence and reservation; settlement as first-verified (the pass verdict on one racer's submission settles the contract and the others' claims are reaped with packets, never silently); the compute cost stated where the opt-in is declared and in the report (racing spend is metered per racer); offline claiming still impossible; the projections and the situation read showing the race; drills: two racers, one verdict, the loser's reap packet, a squad without the opt-in refusing the second claim exactly as today, and every existing chain verifying byte for byte. Phase 13 opens when the Phase 12 exit record merges; plan now, implement as a draft until then (decisions/0003).
