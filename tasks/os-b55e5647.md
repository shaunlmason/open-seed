---
id: os-b55e5647
title: 'next: Phase 13 item 6 — the machine-protocol surface and platform parity including Windows (III.I)'
state: ready
priority: P2
squad: core
created_at: "2026-09-03T02:40:00Z"
updated_at: "2026-09-03T05:24:15Z"
---

Build plan Phase 13 item 6 (docs/next-build-plan.md): the machine-protocol surface exposing the CLI's verbs with identical semantics, and platform parity (including Windows) documented and tested.

Charter: §II.10 (affordances: the CLI is the complete interface; a machine-protocol surface exposes identical semantics), III.I row 3 ("The CLI is the complete interface; the machine-protocol surface exposes identical semantics; platform parity (including Windows) is documented and tested") — the row the Phase 8 exit record routed here (next/docs/progress.md, Phase 8 exit). Deps: Phase 12.

What the tree has: every verb as an envelope (seed-envelope/0) with affordances and the position stamp; the affordance computation and admission sharing one rule set (internal/admit); the proposal protocol of the forge-hosted service (Phase 12 item 2: signed events proposed over HTTP with the same rule set, refusals as envelopes); the CLI drilled on Linux only; no platform-parity statement.

Expected shape, for the plan to settle: the machine protocol as the CLI's verbs over one transport (the plan chooses — the service's HTTP shape extended verb by verb, or a JSON-RPC over stdio — with the rule that a verb exists on the protocol iff it exists on the CLI, drilled by deriving both surfaces' verb sets from one table), identical envelopes and codes, the affordance envelope on every response; platform parity documented per platform (Linux, macOS, Windows) with the CI matrix running the suites on each, path and line-ending handling stated, and the honest list of what a platform cannot do (the pre-receive hook posture on a forge that lacks it). Phase 13 opens when the Phase 12 exit record merges; plan now, implement as a draft until then (decisions/0003).
