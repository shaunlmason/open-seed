---
id: os-f743e94a
title: 'next: boundary tasks pins the field names but not the values, so an out-of-vocabulary state or digest passes the stranger''s read'
state: backlog
priority: P2
squad: core
created_at: "2026-09-05T19:46:59Z"
---

Found by a read-only review of the tree at bfa01638 (2026-09-05). runBoundaryTasks (next/cmd/seed/boundary.go:339-395) checks the four pinned top-level keys via pinned() and then unmarshals into any: it never validates that state is one of the five-state vocabulary, that the positions are integers, or that the artifact digests are well-formed (the shapes are defined at next/internal/boundary/boundary.go:302-322). Plan D2 (plans/os-40ed0ca0.md:60-73) says "Refused: a state the other side could not observe from its own read". Repro: serve {"request":"x","answer":{},"state":"root","artifacts":["not-a-digest"]} with exactly the pinned keys; seed boundary tasks returns an OK envelope. This card: validate each task object against the pinned shapes at the stranger's reader, refusing by name. Tier: standard. Plan-first.
