---
id: os-7599c27d
title: 'next: seed ledger audit — the five-bar audit over any ledger, the shadow run''s evidence for III.R row 5'
state: in_progress
priority: P2
squad: core
claim:
    actor: seed-next-implementer
    token: c-7834130f215b35cb
    claimed_at: "2026-09-03T23:23:05Z"
    lease_expires: "2026-09-04T00:53:05Z"
created_at: "2026-09-03T23:22:31Z"
updated_at: "2026-09-03T23:23:05Z"
---

The promotion evidence packet (os-98ce6f8a, next/docs/promotion.md) names the shadow run's evidence at the window's end: the five-bar audit (chain violations, lost updates, silent abandonments, guardrail breaches, unreserved spend; next/spec/simulation.md) applied to the real chain. Today simulate.Audit (internal/simulate/audit.go) audits any record slice but only seed simulate calls it, over the chain it built itself; no verb runs it over a ledger an operator points at, so the shadow run cannot produce the measurement charter III.R row 5 asks for (the conformance table routes R.5 to exactly this audit over the shadow ledger).

Expected shape, for the plan to settle: seed ledger audit --ledger <dir> [--remote <url>] verifying from genesis (the existing verify path, refusing an invalid chain by its own code before auditing anything) and rendering one envelope: the five bars each with the positions that violate it, clean true only when every list is empty, position-stamped at the tip audited; a clean audit exits 0, a violated one refuses with a refinement of an existing exit code recorded in envelope.md, never a new exit number; the registry and the machine-protocol surface pick the subverb up from the one verb table; drilled with the planted violations the simulate audit drills already use, now through the CLI over a materialized ledger, plus the invalid-chain refusal. Bounds: no admission change, no projection, no change to simulate.Audit's bars. Tier: standard. Deps: none (the packet's protocol names the verb; the verb needs no decision).
