---
id: os-7e197768
title: 'next: loop verbs — Phase 9 item 5 part (c)'
state: backlog
priority: P1
squad: core
created_at: "2026-09-01T04:46:33Z"
---

The third part of build-plan Phase 9 item 5, split from os-52d5da3f for reviewability (its parts (a) obligations projection and (b) situation read landed together; a dozen new CLI verbs in the same diff would be unreviewable). Today a lane acts through the raw protocol seam: ledger append --verb claim.taken --payload {...}, with fences read out of a projection by hand, four-part packets hand-built, reservation ids and base ranges looked up manually. Deliver verbs that DERIVE every argument the system can derive and REFUSE before signing what the tables would refuse after, naming the act that would succeed: the fence from the active window (the situation read already returns it), the reservation id from the budget view, the submission position from the fold, the base range from the repository. Same principle Phase 8 landed for legality (one rule set, enforcement and advertisement) applied to argument construction, so the CLI makes the legal act cheap and the illegal act hard to express. Candidate first set, for the plan to fix: claim/release/park, submit, verdict render (exists), reserve/settle/release, file/specify, block/unblock, message. Scope guards: no new authority, no orchestration layer, success envelopes stay terse (teaching text lives in refusals only), and every verb keeps the envelope contract (position stamp, affordances, attempts journaling at admission-boundary seams). The obligations projection is the natural driver: a verb should be able to say which obligation it discharges. Plan-first (L2).
