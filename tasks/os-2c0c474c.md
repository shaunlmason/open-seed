---
id: os-2c0c474c
title: 'paperclip backend: live-server contract test (npx paperclipai onboard)'
state: review
priority: P3
squad: core
author: shaunlmason
created_at: "2026-08-23T07:26:52Z"
updated_at: "2026-08-24T02:54:04Z"
---

Follow-up card promised by plans/os-e2b3a1f9.md step 3: the adapter's contract test (testdata/fake-paperclip) exercises the API subset offline only, and the plan said the live-server test should be "documented in the README and tracked as a follow-up card if it can't run in CI". The README (.seed/backends/paperclip/README.md, Testing section) still calls it "documented follow-up work" — this is that card.

Scope:
- `npx paperclipai onboard --yes` (embedded Postgres, Node 20+) and run the same nine-verb envelope/exit-code suite the fake server runs, against the real API.
- Reconcile route shapes and behaviors against the live API — the README declares API-drift risk (very high upstream velocity, no shipped SDK). The manifest pins the tested Paperclip version; bump the pin when the test runs.
- Decide the home: CI job, or documented manual procedure — record the choice where the test lives.

Acceptance: every verb round-trips against a real Paperclip instance with the same mapped exit codes as the fake (0/2/3/4/5/6); checkout contention and the reaped-then-reclaimed same-actor token rotation pass natively; route-shape reconciliations or discrepancies are declared in the README, none discovered later.

## Comment cm-a6a6d5aa (shaunlmason, 2026-08-24T02:06:23Z)

Planning complete. Plan PR: https://github.com/shaunlmason/open-seed/pull/56 (plans/os-2c0c474c.md, seed plan lint ok, pr classify class=plan purity ok).

A live probe was run while planning: npx paperclipai@2026.817.0 onboard --yes --data-dir <scratch> comes up fully headless (embedded Postgres, 210 migrations, local_trusted on 127.0.0.1:3100) and serves the API unauthenticated over loopback, so the live test needs no credentials. curl against it surfaced nine drifts (F1-F9 in the plan), two silent: single-issue routes are not company-scoped (adapter 404s), and the field is status not state (adapter PATCH returns 200 and changes nothing). Issues carry no metadata, so the seedToken fence has no storage. The manifest pin the README claims does not exist, and budget=native is sold per-goal while the platform scopes budgets to company and agent.

Implementation is a reconciliation of the adapter's transport layer, larger than the card title implies; the card's acceptance is kept in full. Parked pending plan review.

## Evidence ev-999fbf3a (pr, shaunlmason, 2026-08-24T02:53:53Z)

https://github.com/shaunlmason/open-seed/pull/62

## Comment cm-659acbe9 (shaunlmason, 2026-08-24T02:53:56Z)

Implemented. PR: https://github.com/shaunlmason/open-seed/pull/62 (class=task, purity ok). Receipt: receipts/os-2c0c474c.json, all 8 validation commands exit 0, plan sha256 matches the approved blob.

Acceptance: the full nine-verb corpus is green against a real paperclipai 2026.817.0 (embedded Postgres) AND against the reconciled fake, from one shared corpus.sh. The live suite self-skips offline so make check stays green and credential-free; the onboard path (what CI runs) was verified end to end, exit 0, leaving no server or Postgres behind.

Reconciled against live: routes are not company-scoped (/api/issues/<id>); the field is status not state and a wrong field name 200s while changing nothing (every state assertion now re-reads); issues have no metadata, so bookkeeping moved to the per-issue document store; checkout needs an agent UUID, so --actor resolves against the roster with absent/ambiguous both refusing; deps are blockedByIssueIds/blockedBy; blocked needs an unblockDescriptor; there is no /events route, so taskless events go to the company activity log; comment_id and evidence_id are now emitted per verbs.json.

Two guarantee changes: the document store enforces compare-and-swap, so the fence is now ATOMIC and the check-then-act variance is retired rather than restated. And Paperclip checkout dispatches work rather than locking, so seed-owned agents must be paused; live-test has a quiescence gate that refuses to run rather than reporting the flake as a contract failure.

Corrections: budget=native is company/agent scoped, not per goal; the manifest pin the README claimed existed did not, and now does.
