---
id: os-2c0c474c
title: 'paperclip backend: live-server contract test (npx paperclipai onboard)'
state: backlog
priority: P3
squad: core
created_at: "2026-08-23T07:26:52Z"
---

Follow-up card promised by plans/os-e2b3a1f9.md step 3: the adapter's contract test (testdata/fake-paperclip) exercises the API subset offline only, and the plan said the live-server test should be "documented in the README and tracked as a follow-up card if it can't run in CI". The README (.seed/backends/paperclip/README.md, Testing section) still calls it "documented follow-up work" — this is that card.

Scope:
- `npx paperclipai onboard --yes` (embedded Postgres, Node 20+) and run the same nine-verb envelope/exit-code suite the fake server runs, against the real API.
- Reconcile route shapes and behaviors against the live API — the README declares API-drift risk (very high upstream velocity, no shipped SDK). The manifest pins the tested Paperclip version; bump the pin when the test runs.
- Decide the home: CI job, or documented manual procedure — record the choice where the test lives.

Acceptance: every verb round-trips against a real Paperclip instance with the same mapped exit codes as the fake (0/2/3/4/5/6); checkout contention and the reaped-then-reclaimed same-actor token rotation pass natively; route-shape reconciliations or discrepancies are declared in the README, none discovered later.
