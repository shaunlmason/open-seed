---
id: os-32d06c65
title: 'next: revocation reaps the revoked holder''s open claims on the revocation alone (III.E row 8''s reap arm; Phase 12 item 1 follow-up)'
state: ready
priority: P2
squad: core
created_at: "2026-09-02T23:55:47Z"
updated_at: "2026-09-03T03:34:25Z"
---

Charter III.E row 8's reap arm (revocation ... reaps its claims) is unmet in the tree, found by the Phase 10 exit walk and routed to Phase 12 item 1 in docs/next-build-plan.md. Today the maintenance reap needs an admitted run.interrupted answered by an expired or wedged classification (next/spec/maintenance.md), and a revoked holder's no_data stream has no reap path at all, so a revoked key's open claims stand until someone notices. The revocation record is the one case where the ledger itself, not the observation channel, corroborates the reap: a revoked holder can never exit its window, because every proposal from that key refuses at the boundary from the revocation's position on. Scope: claim.reaped admitted on the revocation alone (the reaper citing the actor.revoked position as its corroboration, no run.interrupted required), the maintenance pass reaping every open claim a revocation orphans with an honest force-reap packet (executors.md's force path) so the work is re-offered, the obligation surfacing it, and the drills at the boundary and in the maintenance fixture. Plans/os-465e356e.md D9 names this card as the drill's follow-up: the compromised-actor drill asserts today's consequence (a revoked key's proposals refuse at the push; its contract branch closes with its standing) and this card's drill extends its corpus when it lands. Plan-first (core squad, L2).
