---
id: os-768361cc
title: 'next: promotion milestone — define spin-out criteria and the critical path'
state: in_progress
priority: P1
squad: core
claim:
    actor: seed-next-implementer
    token: c-d37d5c122f3669be
    claimed_at: "2026-09-01T03:15:29Z"
    lease_expires: "2026-09-01T04:15:29Z"
created_at: "2026-09-01T03:13:41Z"
updated_at: "2026-09-01T03:15:29Z"
---

The build plan names spin-out twice (ground rules: v1 scripts/seed stays the coordination entry point 'until spin-out'; autonomy contract: publishing/spin-out is an escalation) but never DEFINES it: no criteria, no phase, no critical path, no cutover or rollback. Thirteen phases of conformance work therefore have no stated finish line for 'Seed can take over', which is the operator's actual goal. Amend docs/next-build-plan.md with a promotion section (plan-gated; the #157 rule that routing binds only when the phasing authority says it). PROPOSED CONTENT, for the plan to refine: (1) DEFINITION — promotion is two steps: self-hosting (Seed coordinates this repository's own development, v1 retained read-only for history) and distribution (Seed is what new users clone). (2) CRITERIA — loop-completeness: every act the daily loop requires is a first-class Seed verb rather than a hand-assembled ledger append (file, specify, claim, plan-gate, progress, submit, verdict, merge-observe, park, escalate, mail, and a situation/resume read), because today the CLI is the raw protocol seam and a lane cannot run the loop ergonomically on it; lanes operable (Phase 9: six role fragments, dispatcher least-capability, worker loop with exhaustion parking, escalation, maintenance unattended); migration proven (Phase 12 item 5 import drilled against a real v1 export); a shadow run in which Seed coordinates a real slice of this repository's own cards beside v1 with divergence reconciled; cutover and rollback written down (entry-point flip, what stays authoritative where, how to fall back); core conformance at the enforced self-hosted posture with doctor reporting which Phase 13 rows remain open. (3) EXPLICITLY NOT REQUIRED — Phase 10 (eval infrastructure), Phase 11 (curator and flywheel), and Phase 13 (deferred conformance: racing mode, remaining adapters, federation) continue AFTER promotion; the plan already frames Phases 0-12 as core conformance with 13 deferred, so promoting at core conformance is consistent with its own structure rather than a weakening of it. (4) CRITICAL PATH — Phase 9 (operability) then the loop-verb surface then the Phase 12 subset (preseed, import, docs/simulation) then shadow run then cutover; state which phases are parallelizable. Also add a pointer from Phase 9 and Phase 12 to the section so a phase reader sees the finish line. Plan-first (L2).
