---
id: os-0f924157
title: 'next: the enforced hook reads the declaration at the default branch''s tip, so the ceiling refuses at the boundary (review finding on #323)'
state: ready
priority: P1
squad: core
created_at: "2026-09-05T01:08:50Z"
updated_at: "2026-09-05T05:16:05Z"
---

Review on #323 (os-b5051f2e) found that seed-admit's ledger half never reads a declaration: the installed pre-receive script is exec <bin> pre-receive with no environment (next/internal/simulate/hook.go), the binary reads only SEED_ADMIT_REF and SEED_PUSHER (next/cmd/seed-admit/main.go, coderef.go), admitUpdate builds every context as admit.ContextOver(records[:i]) with no admit.WithDeclaration, and the ceiling rule returns nil without a declaration (next/internal/admit/admit.go). coderef.go already parses seed.json from the default branch's tip for the code-ref half but never hands it to admitUpdate; seed-admit serve shares the gap. Reproduced under simulate --posture enforced-self-hosted: an agent-kind key's raw claim.taken on a standard contract lands at the hooked remote (no --config), while the same claim under --config refuses tier_above_ceiling at the client and ledger audit --config names the breach. next/spec/postures.md already says the hook reads the declaration from the default branch's tip, and plans/os-0d4f2af3.md AC3 promised the ceiling refuses at the hook and the CLI; nothing drills it. This card: admitUpdate (and serve) read the declaration at the default branch's tip and pass admit.WithDeclaration, a raw above-ceiling push drill in cmd/seed-admit that sees tier_above_ceiling at the enforced boundary, the redteam one-derivation fixture given the same declaration on its in-process side, and simulation.md's declared paragraph tightened back to 'at every claim'. Out of #323's file scope (plans/os-b5051f2e.md binds Nothing else; cmd/seed-admit is the protected surface, plans/os-0d4f2af3.md D5, next/spec/admission.md). Tier: standard. Plan-first.
