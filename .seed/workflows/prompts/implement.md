Implement the approved plan this step consumes. Work in the current
worktree; any task-state change goes through `scripts/seed task <verb>`.
Write a change summary (what/why/validation run) to the declared produce
path.
