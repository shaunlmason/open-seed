# Plan: next — boundary check surfaces a canonicalization failure (os-1c284ba8)

Follow-up to Phase 13 item 5 (#279, os-40ed0ca0). Copilot's finding on
the landing PR was fixed on the branch after the PR had merged, so the
fix follows as its own card: `seed boundary check` drops both
`Card.Canonical()` errors, so a failure on either side compares nil
with nil and reports no drift. Tier: trivial.

## What the tree actually shows

- `next/cmd/seed/boundary.go`'s check renders the declaration's card
  and compares canonical bytes with the checked-in card's, `gotCanon, _`
  and `wantCanon, _` (#279). `Canonical` fails only when the card
  cannot be marshaled, which no parsed card does today, which is why
  the drills never saw it; the shape is still wrong.

## Design decisions (binding for this task)

- **D1 — each side fails by name.** A canonicalization error on the
  checked-in card fails `unavailable` (exit 5, the envelope's existing
  operational failure) naming the card path; one on the rendered card
  fails `unavailable` naming the rendered card. No new exit code, no
  spec change: `envelope.md` already lists exit 5.
- **D2 — no refactor.** The fix is the two error branches and nothing
  else in the file.

## Steps

1. The two branches in `next/cmd/seed/boundary.go`.
2. Receipt; evidence; review.

## File Scope

- `next/cmd/seed/boundary.go`
- `receipts/os-1c284ba8.json`

Nothing else.

## Acceptance Criteria

**Boundary set (new, shown working):**

1. `boundary check` never compares nil with nil: a canonicalization
   error on either side fails `unavailable` naming the side.
2. `make check` green; no model identifiers in any committed artifact.

**Retention set (existing, shown unharmed):**

- The boundary drills pass unchanged: a card that renders and parses
  checks exactly as before, drift and signature refusals included.

## Validation Commands

- Boundary: `cd next && go test ./cmd/seed/ -run Boundary -count=1`
- Retention: `make check` (exit checked separately from any pipe)

## Expected diff shape

Modified: `next/cmd/seed/boundary.go` (eight lines), the receipt.
