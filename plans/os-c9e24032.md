# Plan: next Phase 7 exit — frontier update (os-c9e24032)

Records the Phase 7 exit in the frontier file. Design authority:
[`docs/next-build-plan.md`](../docs/next-build-plan.md) Phase 7 *Exit*
("charter III.H items for the implemented adapter — poll-only run,
reservation race drill, disposability drill (randomized kill after
sync; complete elsewhere)") and the [`AGENTS.md`](../AGENTS.md) rule
that `next/docs/progress.md` stays the accurate frontier fresh agents
resume from. Every Phase 7 item is merged and every Phase 7 card
closed (7.1 #145 against plans #144/#146, 7.2 #149 against #147/#148,
7.3 #151 with follow-up #152 against #150, 7.4 #154 against #153);
the file still shows 7.4 as in review and the frontier pointing at
landing it — stale exactly where a fresh agent would read it.
Docs-only: no code surface changes. The precedent is the Phase 5 and
6 exit records (plans/os-6e37b10e.md, plans/os-600be59e.md); like
them, this administrative card appears in the exit prose, never as a
ledger row.

## Steps

1. **Flip the finished row.** In `next/docs/progress.md`, mark 7.4
   **done** (merged #154; card closed; the review round hardened the
   validity helpers to re-run each verb's strict payload decode at
   the fact's own position and gated once-per-fence settles on
   boundary validity, and indexed the interrupts cache table),
   matching the wording of the already-done rows.
2. **Record the Phase 7 exit.** One paragraph in the established
   scoped-exit form: "Phase 7 exit (the III.H subset
   docs/next-build-plan.md scopes): met." Tie the exit line's three
   named drills to evidence — the wakeless poll-only run through the
   whole loop (#145: wake is advisory transport, its total failure
   costs only latency), the reservation race drill (#149: two drafts
   admission-checked against one view, the second refusing at
   append, concurrent over-spend structurally impossible), and the
   disposability drill (#151: a real worker subprocess SIGKILLed at
   a seeded randomized site after an admitted synchronization, the
   chain verifying, the contract completing elsewhere, the loss
   window exactly the post-sync observation lines) — then the III.H
   rows the merged PRs evidence for the implemented adapter:
   offers-and-claims scheduling with eligibility scoping, expiry,
   no assignments to orphan, and exclusivity settling at admission
   (#145); spending verbs requiring an admitted reserve with
   execution fenced to the reservation and actuals recorded by
   settle/release (#149, #151's run.started filling the spending
   table and Provision refusing outside the gate, #152
   authenticating folded starts position-accurately); metering on
   the observation channel settling at run end with no unmetered
   execution path (#151, #152); the public adapter interface with
   the local worktree adapter (#151, #152); and preemption
   graceful-first with safe-point semantics specified as the worker
   contract and a force kill still yielding an honest reap packet
   (#154 — landed beyond the exit line's three named drills, so the
   III.H preemption row closes with the phase rather than waiting).
   Note that the local worktree adapter can be stopped
   synchronously, so the risk-limit posture `next/spec/budgets.md`
   declares has no declaration to make for it yet. Close with the
   unmet III.H remainder by landing phase: the remaining container,
   cloud-session, and enrolled-remote adapters with their
   per-adapter risk-limit declarations (Phase 13 item 2); the
   remaining-reservation budget block in the worker's envelope and
   exhaustion parking through the envelope-driven loop (Phase 8
   item 1, exercised by the Phase 9 lanes); and scheduling inputs
   completing with routing-as-data in the Phase 9 dispatcher and
   qualification tuples in Phase 10 item 1 (cost classes exist
   today in the filed budget class).
3. **Point the frontier at Phase 8.** Rewrite the frontier
   paragraph: Phases 0 through 7 done and closed, next action
   Phase 8 item 1 (the affordance envelope computed from the same
   `internal/admit` rule set, with `position`, budget block,
   structured errors, and exit codes per spec) — a new card,
   plan-first, per the loop; then Phase 8 items 2 and 3 (the
   affordance-refused-equals-bug regression class, the refusal-rate
   report metric).

## File Scope

- `next/docs/progress.md`
- `receipts/os-c9e24032.json`

## Acceptance Criteria

1. The 7.4 row reads done with its PR and review-round summary; no
   row still claims review or backlog state for a merged Phase 7
   item.
2. The Phase 7 exit paragraph exists in the scoped-exit form, cites
   the three named drills with their PRs, maps the III.H rows the
   phase met for the implemented adapter, and routes every unmet
   III.H row to its named landing phase.
3. The frontier names Phase 8 item 1 as the next action and stops
   pointing at any Phase 7 work.
4. `make check` green (docs-only, so unchanged code surfaces).

## Validation Commands

```sh
make check
```

## Expected diff shape

One file, `next/docs/progress.md`: a row flip, one exit paragraph,
and a rewritten frontier — plus the receipt. Nothing else.
