# Plan: next Phase 7 exit — frontier update (os-c9e24032)

Records the Phase 7 exit in the frontier file. Design authority:
[`docs/next-build-plan.md`](../docs/next-build-plan.md) Phase 7 *Exit*
("charter III.H items for the implemented adapter — poll-only run,
reservation race drill, disposability drill (randomized kill after
sync; complete elsewhere)") and the [`AGENTS.md`](../AGENTS.md) rule
that `next/docs/progress.md` stays the accurate frontier fresh agents
resume from. Every Phase 7 item is merged and every Phase 7 card
closed (7.1 #145 against plans #144/#146, 7.2 #149 against #147/#148,
7.3 #151 with follow-up #152 against #150, 7.4 #154 against #153);
the file still shows 7.4 as in review and the frontier pointing at
landing it — stale exactly where a fresh agent would read it.
Docs-only: no code surface changes. The precedent is the Phase 5 and
6 exit records (plans/os-6e37b10e.md, plans/os-600be59e.md); like
them, this administrative card appears in the exit prose, never as a
ledger row.

## Steps

1. **Flip the finished row.** In `next/docs/progress.md`, mark 7.4
   **done** (merged #154; card closed; the review round hardened the
   validity helpers to re-run each verb's strict payload decode at
   the fact's own position and gated once-per-fence settles on
   boundary validity, and indexed the interrupts cache table),
   matching the wording of the already-done rows.
2. **Record the Phase 7 exit.** One paragraph in the established
   scoped-exit form: "Phase 7 exit (the III.H subset
   docs/next-build-plan.md scopes): met." Tie the exit line's three
   named drills to evidence — the wakeless poll-only run through the
   whole loop (#145: wake is advisory transport, its total failure
   costs only latency), the reservation race drill (#149: two drafts
   admission-checked against one view, the second refusing at
   append, concurrent over-spend structurally impossible), and the
   disposability drill (#151: a real worker subprocess SIGKILLed at
   a seeded randomized site after an admitted synchronization, the
   chain verifying, the contract completing elsewhere, the loss
   window exactly the post-sync observation lines) — then the III.H
   rows the merged PRs evidence for the implemented adapter:
   offers-and-claims scheduling with eligibility scoping, expiry,
   no assignments to orphan, and exclusivity settling at admission
   (#145); spending verbs requiring an admitted reserve with
   execution fenced to the reservation and actuals recorded by
   settle/release (#149, #151's run.started filling the spending
   table and Provision refusing outside the gate, #152
   authenticating folded starts position-accurately); metering on
   the observation channel settling at run end via `run.settled`,
   drilled through the full bracket (#151, #152) — claimed with the
   same caller-contract honesty as disposal, not as an enforced
   impossibility: `Meter` is the worker contract's obligation and
   nothing yet makes an unmetered caller structurally impossible
   (budget consumption is enforced upstream at the reservation
   gate), so metering completeness is enforced by detection, routed
   below as a named maintenance-lint obligation; the public
   adapter interface with
   the local worktree adapter (#151, #152); and preemption
   graceful-first with safe-point semantics specified as the worker
   contract and a force kill still yielding an honest reap packet
   (#154 — landed beyond the exit line's three named drills, so the
   III.H preemption row closes with the phase rather than waiting).
   Note that the local worktree adapter can be stopped
   synchronously, so the risk-limit posture `next/spec/budgets.md`
   declares has no declaration to make for it yet. Close with the
   unmet III.H remainder by landing phase, each named as a binding
   obligation of the phase it routes to: the remaining container,
   cloud-session, and enrolled-remote adapters with their
   per-adapter risk-limit declarations (Phase 13 item 2); the
   remaining-reservation budget block in the worker's envelope
   (Phase 8 item 1); **exhaustion parking — the worker loop that
   answers the budget gate's structured refusal by taking the 7.4
   `claim.parked` exit with its packet — to Phase 9 item 1, whose
   build-plan item text this task extends to name the worker-lane
   loop and that obligation explicitly** (review finding on the
   task PR: a routing is binding only when the phasing authority's
   own item says it, not when the exit record asserts it; the
   parking mechanics and safe-point contract are 7.4's, already
   merged — what routes forward is the lane behavior that invokes
   them); **metering-completeness detection to Phase 9 item 3,
   whose maintenance-lint list this task likewise extends by name**
   (a closed claim window carrying an admitted `run.started` with
   no `run.settled` is flagged); and scheduling inputs completing
   with routing-as-data in the Phase 9 dispatcher and qualification
   tuples in Phase 10 item 1 (cost classes exist today in the filed
   budget class).
   `next/spec/budgets.md` currently forward-assigns the
   exhaustion park to "Phase 7.4's worker behavior" — a 7.2-era
   guess the phasing authority never adopted (build-plan Phase 7
   item 4 and the approved plan #153 scope 7.4 to interrupt-driven
   preemption, and #154 merged on that scope) — so this task also
   corrects that one sentence to the binding routing above, keeping
   spec and frontier telling the same story.
3. **Name the routed obligations in the build plan itself.** In
   `docs/next-build-plan.md`, extend Phase 9 item 1: the worker
   lane's role definition includes its loop (poll, claim, work,
   meter, sync, deliberate exit), and exhaustion parking is part of
   that loop — a budget refusal at a spending gate triggers the
   `claim.parked` exit with packet (the III.H row the Phase 7 exit
   routes here), consuming Phase 8's envelope budget block. Extend
   Phase 9 item 3's lint list: unsettled-run detection — a closed
   claim window carrying an admitted `run.started` with no
   `run.settled` is flagged (the Phase 7 exit's
   metering-completeness obligation). Two surgical sentence-level
   extensions; no phase, dependency, or exit line changes.
4. **Correct the budgets spec's stale forward reference.** In
   `next/spec/budgets.md`, replace the sentence assigning the
   exhaustion park to "Phase 7.4's worker behavior" with the
   binding routing: exhaustion's structured refusal is landed
   (7.2), the parking mechanics and safe-point contract are landed
   (7.4), and the worker-lane loop that parks on exhaustion is
   named in `docs/next-build-plan.md` Phase 9 item 1, consuming
   Phase 8's envelope `{reserved, remaining}` block.
5. **Point the frontier at Phase 8.** Rewrite the frontier
   paragraph: Phases 0 through 7 done and closed, next action
   Phase 8 item 1 (the affordance envelope computed from the same
   `internal/admit` rule set, with `position`, budget block,
   structured errors, and exit codes per spec) — a new card,
   plan-first, per the loop; then Phase 8 items 2 and 3 (the
   affordance-refused-equals-bug regression class, the refusal-rate
   report metric); with the exhaustion-park obligation named on
   the Phase 9 line so the routing survives into the next phase's
   reading of the frontier.

## File Scope

- `next/docs/progress.md`
- `docs/next-build-plan.md` (two sentence-level extensions to
  Phase 9 items 1 and 3; no phase, dependency, or exit changes)
- `next/spec/budgets.md` (the one forward-reference sentence)
- `receipts/os-c9e24032.json`

## Acceptance Criteria

1. The 7.4 row reads done with its PR and review-round summary; no
   row still claims review or backlog state for a merged Phase 7
   item.
2. The Phase 7 exit paragraph exists in the scoped-exit form, cites
   the three named drills with their PRs, maps the III.H rows the
   phase met for the implemented adapter — the metering row claimed
   as the drilled caller contract it is, never as an enforced
   impossibility — and routes every unmet III.H row to an
   obligation NAMED IN the landing phase's own build-plan item
   text: exhaustion parking in Phase 9 item 1, unsettled-run
   detection in Phase 9 item 3.
3. `docs/next-build-plan.md` Phase 9 items 1 and 3 carry the two
   obligations in their own words; `next/spec/budgets.md` no longer
   assigns the exhaustion park to 7.4: build plan, spec, and
   frontier tell the same routing story.
4. The frontier names Phase 8 item 1 as the next action, carries
   the exhaustion-park obligation on its Phase 9 line, and stops
   pointing at any Phase 7 work.
5. `make check` green (docs-only, so unchanged code surfaces).

## Validation Commands

```sh
make check
```

## Expected diff shape

Three files: `next/docs/progress.md` (row flips, one exit
paragraph, a rewritten frontier), `docs/next-build-plan.md` (two
sentence-level extensions to Phase 9 items 1 and 3), and
`next/spec/budgets.md` (one corrected forward-reference sentence) —
plus the receipt. Nothing else.
