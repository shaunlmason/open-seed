# Plan: knowledge search, an advisory BM25 read over the curation stores (os-405d3b20)

**Retrospective plan.** This card was claimed and implemented straight from
its body, and landed through the ordinary task PR (#342), but no plan PR was
authored first, so `plans/os-405d3b20.md` never existed. That skipped step is
the gap this file records: above the trivial tier a plan is merged before
implementation starts, and here it was not. The card was closed with a plain
`accept` and does not carry the D7 no-PR exemption, so this file is also the
D3 plan-resolvability artifact the done-consistency lint requires. It
documents what was built and how it was verified. It does **not**
retroactively confer plan approval: the authorization of record is the human
`accept` already on the card, and this file exists so the lint can resolve
`plans/<task-id>.md`. It gates no future work.

The surfacing set (promoted lessons, standing dead ends, named markdown docs)
was reachable only by its applies-when predicate, so an agent holding a
question rather than a routing tuple had no read. The card added a lexical
BM25 ranking over that same set as an **advisory** read: it surfaces, it never
delivers. Nothing routes, admits, or gates on a search result, and the
applies-when path is untouched.

## Steps

1. **Reimplement BM25 in `next/internal/curation`**: tokenizer, per-field
   length normalization, the `k1`/`b` parameters, and a deterministic ranking
   over lessons, dead ends and named docs, with no new dependency.
2. **Expose it as `seed knowledge search`**, registered in the CLI catalog
   beside the other `knowledge` subverbs, returning ranked citations and never
   a delivery decision.
3. **Drill it**: unit tests over the ranking and its determinism, plus a CLI
   test that a search result changes no lifecycle or routing state.
4. **Record it** in `next/spec/curation.md`, the decision log and the frontier.

## File Scope

- `next/internal/curation/search.go` and its test (the ranking).
- `next/cmd/seed/knowledge.go`, `next/cmd/seed/catalog.go` and the CLI tests
  (the verb).
- `next/spec/curation.md`, `next/docs/decisions.md`, `next/docs/progress.md`,
  `memory/LEARNINGS.md` (the record).

No conformance row, no protocol version, no admission rule: an advisory read
moves none of them.

## Acceptance Criteria

- `seed knowledge search` ranks the surfacing set lexically and returns
  citations, deterministically for a fixed corpus and query.
- The applies-when surfacing path is unchanged, and no search result feeds
  routing, admission, or a delivery decision.
- No new module dependency; the ranking is the tree's own code.
- `make check` green, the coverage gate held.

## Validation Commands

- `cd next && go test ./internal/curation/... -run 'Search|BM25' -count=1`:
  the ranking's own drills, including determinism.
- `cd next && go test ./cmd/seed -run 'Search|Lessons' -count=1`: the verb at
  the terminal, and that a search moves no state.
- `cd next && go run ./cmd/seed knowledge search --help`: the verb is
  registered in the catalog both surfaces are drawn from.
- `git show 74d32c8 --stat`: the ten files above and nothing else.
- Evidence record: card `os-405d3b20` review block, accepted by `shaunlmason`
  2026-09-07T06:26:06Z, evidence PR #342 merged as `74d32c8`.
