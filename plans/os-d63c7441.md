# Plan: next — the Phase 13 exit record (os-d63c7441)

Phase 13 (conformance completion) is complete once its seven items
and the conformance report card (os-83bc3d84) merge, and this card is
its exit record: the paragraph in `next/docs/progress.md` that the
Phase 5 through 12 records wrote, confirming the exit line's
criteria — the named III.F, III.H, III.I and III.N criteria green,
III.J rows 2 and 3 beside III.I, and the conformance report showing
Part III complete at the enforced self-hosted posture — row by row,
by citation to drills on `main`, and flipping the conformance table's
Phase 13 rows from `open` to what the tree shows, with this record as
their evidence.

**Why plan-first for a docs-and-table change.** The Phase 10, 11 and
12 records were planned first (os-a026f5ea, os-efb2a099, os-9e9ae30c)
because the record is the document the next step orients from; this
one is what the promotion criteria (build plan §5) read, so it
deserves a reviewer before it becomes the thing everyone reads.

## What the tree actually shows

Every numbered item has an implementation on `main` or in review at
writing time, cited here so the record cites rather than asserts:

| item | what | landed |
|---|---|---|
| 1 | racing mode as the per-squad opt-in with first-verified settlement (III.F row 7) | #269 (plan #256) |
| 2 | the remaining executor adapters: container, cloud agent session, enrolled remote worker (III.H) | #282 (plan #274), in review |
| 3 | the non-primary forge adapter, Forgejo, behind the forge seam (III.N row 2) | #281 (plan #275), in review |
| 4 | request ingress, federation as uniform read remotes, the mirror arm of the injection corpus (III.N row 4, III.J row 2) | #270 (plan #257) |
| 5 | the A2A-shaped cross-organization boundary (III.N row 3) | #279 (plan #258); the canonicalization follow-up os-1c284ba8 (plan #285) |
| 6 | the machine-protocol surface with identical semantics and platform parity including Windows (III.I row 3) | #273 (plan #261, amendment #278), in review |
| 7 | tuple ranking as supervisor policy, the planner lane's offers carrying the strongest (III.J row 3) | #286 (plan #276), in review |
| gaps | `ledger show` stamping its `chain_invalid` position (#265); the cache carrying the event's `ts` (#266) | merged |
| report | the conformance report: the Part III table, rendered and drift-gated, read by the doctor | os-83bc3d84, planned |

**The exit line's criteria**, each to be backed by a named drill on
`main` at writing time:

- *III.F*: the lifecycle rows met at Phases 5 and 6, and row 7
  (racing) by #269: `TestRacingOptInAndCap`, the settlement and reap
  drills the item's bullet names; the record cites the test names as
  they stand on `main`.
- *III.H*: the supervisor rows met at Phase 7, and the executor rows
  by #282: the container, cloud and enrolled-remote adapters against
  the one `Adapter` seam with the report's adapters section; the
  record names each adapter's drill.
- *III.I*: rows 1, 2, 4 and 5 met at Phases 8, 10 and 11; row 3 by
  #273 (`TestRegistryMirrorsTheCLIVocabulary`,
  `TestProtocolMethodsAreTheCLIVerbs`,
  `TestServeReturnsTheCLIEnvelopeVerbatim`, the three-platform
  matrix with the Windows leg green) beside #272's generated docs.
- *III.N*: row 1 by #272 (harness fan-outs and generated lifecycle
  docs with drift-failing CI; `last-verified` stamps as the specs'
  status lines), row 2 by #281, row 3 by #279 (`TestTwoOrganizationsAcrossTheBoundary`,
  `TestBoundaryRefusals`), row 4 by #270 (`TestNoHostileRequestWidensTheDispatcherSet`,
  the federation report drill), row 5 by the tuple model and the
  adapters' absence of any vendor dependency (the record asserts it
  by grep, as the Phase 12 record asserted the absence of telemetry).
- *III.J rows 2 and 3*: row 2 closed by the Phase 12 record (#270);
  row 3's policy clause by #286 (`TestRankingOrdersByScoreThenLatestThenCanonical`,
  `TestOfferStrongestFillsTheScopeByPolicy`,
  `TestDueOffersCarryTheStrongestByPolicy`), its metrics half at
  Phase 10 item 5.
- *The conformance report*: `seed doctor` on the fixture deployment
  at the enforced posture reporting `complete: true` with no open
  row outside III.R, and `next/docs/generated/conformance.md`
  regenerated from the flipped table under `docs check`.

## The rows earlier records routed here

| row | routed by | what this record does |
|---|---|---|
| III.I row 3 | Phase 8, held PARTIAL at Phase 12 | met by #273 and #272; closed |
| III.L row 4 (per-verb policy on the machine-protocol surface with attributable approvals) | Phase 12, UNMET | judged against #273: the registry is the one verb table, every method runs the CLI's own run function under the same admission, and approvals are ledger facts attributable to their signer; met if the drills say so, else re-routed with a card |
| III.C row 4 (the contention benchmark at target scale) | Phase 12, PARTIAL | stays `routed` to the backlog's scheduled run; the table says so |
| III.Q row 7 (self-hosting total once bootstrapped) | Phase 12, PARTIAL | stays `routed` to promotion (§5); the table says so |
| III.R (the autonomy end-state) | never claimed | stays `open`: outcomes promotion measures, not mechanisms a phase lands |

## Design decisions (binding for this task)

- **D1 — the record follows the Phase 12 shape exactly.** One bold
  opening naming the exit line's criteria and the drills that back
  them; the pillars walked row by row with "met by" citations; the
  routed rows resolved or re-routed in those words; the closing
  sentence naming this card as the record's task PR.
- **D2 — the table is flipped by the record and nothing else.** Every
  Phase 13 row in `next/spec/conformance.json` moves from `open` to
  the status the record states, with the record as evidence, and
  `conformance.md` is regenerated; a row the record leaves `partial`
  or `routed` carries the routing in its note. No row is flipped
  without a sentence in the record that says why.
- **D3 — complete means what the doctor says.** The record quotes
  the doctor's `conformance` section on the fixture deployment at the
  enforced posture; if any row outside III.R is still `open` the
  record says UNMET and the phase does not close.
- **D4 — the frontier is re-derived toward promotion.** With Phase 13
  closed, the Frontier states that Phases 0 through 13 are done and
  that the destination is §5's two cutovers, neither autonomously
  decidable: the next action is the promotion evidence, presented and
  stopped at, not claimed.
- **D5 — one learning sentence.** What the phase learned, pointing at
  `memory/LEARNINGS.md`.

## Steps

1. Wait for items 2, 3, 6, 7 and os-83bc3d84 to merge (the record
   cites `main`).
2. Write the record into `next/docs/progress.md`; flip the table;
   regenerate `conformance.md`; re-derive the Frontier.
3. Receipt; evidence; review.

## File Scope

- `next/docs/progress.md`
- `next/spec/conformance.json`, `next/docs/generated/conformance.md`
- `docs/next-build-plan.md` (one backlog line, if a re-routing needs it)
- `receipts/os-d63c7441.json`

Nothing else. NOT `next/spec/*.md`, NOT `next/internal/**`, NOT
`next/cmd/**`, NOT `.seed/**`.

## Acceptance Criteria

**Boundary set (new, shown working):**

1. The record names every exit-line criterion with a drill on `main`
   by test name and file, and every Phase 13 row of the table with its
   final status and citation; PARTIAL and routed rows carry a routing.
2. The doctor reports `complete: true` at the enforced posture on the
   fixture deployment, quoted in the record.
3. The Frontier is re-derived from the items and cards, not edited.

**Retention set (existing, shown unharmed):**

- `make check` green (the coverage gate unchanged by a docs-and-table
  change); `docs check` clean; no model identifiers in any committed
  artifact.

## Validation Commands

- Docs: `cd next && go test ./internal/conformance/ ./internal/docs/ ./internal/loopverb/ -count=1` (the table, the rendering and the spec-parity drills)
- Retention: `make check` (exit checked separately from any pipe)

## Expected diff shape

Modified: `next/docs/progress.md` (the record and the Frontier),
`next/spec/conformance.json` (the Phase 13 rows), the regenerated
`next/docs/generated/conformance.md`, possibly one line of
`docs/next-build-plan.md`, the receipt. Nothing else.
