# Plan: next Phase 5 exit — frontier update (os-6e37b10e)

Records the Phase 5 exit in the frontier file. Design authority:
[`docs/next-build-plan.md`](../docs/next-build-plan.md) Phase 5 *Exit*
(charter III.F items except sealed checks) and the [`AGENTS.md`](../AGENTS.md)
rule that `next/docs/progress.md` stays the accurate frontier fresh agents
resume from. Every Phase 5 item is merged and every Phase 5 card closed
(5.1 #122, 5.2 #123, 5.3 #124, 5.4 #125, 5.5 #126, 5.6 #131, follow-ups
#127/#128/#129, engine pin #130); the file still shows 5.6 as "this PR",
5.3 pending #127, and the Phase 5 header as in progress — stale exactly
where a fresh agent would read it. Docs-only: no code surface changes.
The precedent is the Phase 1 exit record (plans/os-beac85e1.md); like it,
this administrative card appears in the exit prose, never as a ledger row.

## Steps

1. **Flip the finished rows.** In `next/docs/progress.md`, mark 5.3
   **done** (merged #124; card closed; the resume-drill and packet-shape
   hardening landed via follow-up #127) and 5.6 **done** (merged #131;
   card closed), matching the wording of the already-done rows.
2. **Tidy the ledger structure.** Move the 4.4 row (os-8d5e9c45,
   re-landed #118) from the Phase 5 section back to the end of the
   Phase 4 section where it belongs, and retitle the Phase 5 header to
   the standard form used by Phases 2–4
   (`## Phase 5 — Lifecycle, claims, packets (docs/next-build-plan.md
   Phase 5; deps: 3 ✓, 4 ✓)`), dropping the in-progress marker.
3. **Record the Phase 5 exit.** One paragraph tying the build plan's
   exit line to evidence: the three named drills — claim race storm
   green on main (#123), packet lint plus the A/B resume fixture drill
   with dead ends never re-tried and the unfinished work performed and
   landed (#124, hardened #127), offline-boundary refusals for exclusive
   verbs (#123) — plus the III.F items each merged PR evidences
   (#122/#129 self-validating table and claim-as-transition; #123/#128
   admission-granted fences, structured contention, stale-fence
   refusals; #124/#127 four deliberate exits with four-part bounded
   packets; #125 acceptance-spec completeness, gate evidence bound to
   the ref revision, propose-never-become; #126 falsifiable-plan lint
   and plan-gated submission; #131 observations with expiry vs. wedge
   distinct in the report). Close with the still-unmet III.F remainder
   by landing phase: sealed checks and their honest-scope documentation
   (Phase 6, the exit's named carve-out); run-anywhere spec-gate
   enforcement (with verdicts, Phase 6); racing mode as the per-squad
   opt-in (Phase 13 item 1); the dependency-cascade bullet (advisory
   wakes land Phase 7; the remainder has no named phase item and falls
   to Phase 13's catch-all).
4. **Open the Phase 6 ledger.** List the four filed verdict-pipeline
   cards (6.1 os-f6d2c267, 6.2 os-6cdc15be, 6.3 os-3128535a,
   6.4 os-d2497eb7) — one line per build-plan item, only those four —
   and rewrite the Frontier paragraph to point at 6.1
   (`submission.made`, verifier workspace, receipt computation,
   `verdict.rendered` at L1 independence; plan-first) and the Phase 6
   exit subset (charter III.G minus L2/L3 and rubric calibration).
5. **Durable insight.** Append to `memory/LEARNINGS.md`: the receipt
   gate makes every task PR plan-first in practice regardless of tier —
   `receipt generate`/`verify` refuse without a merged plan at
   merge-base (D3), so even L1 administrative cards route through a
   plan PR; budget accordingly instead of discovering it at receipt
   time.

## File Scope

- `next/docs/progress.md`
- `memory/LEARNINGS.md`

## Acceptance Criteria

**Boundary set (new, shown working):**

- Every Phase 5 row reads **done** with its merged task PR and closed
  card; no row claims an open review, a pending follow-up, or "this
  PR".
- The Phase 4 ledger contains the 4.4 row; the Phase 5 section contains
  only 5.x rows plus the exit paragraph, under the standard header.
- The Phase 5 exit paragraph names the build plan's three drills with
  their PRs and enumerates the unmet III.F remainder by landing phase.
- The Phase 6 section lists exactly the four filed cards (this card
  appears only in the exit prose), and the Frontier paragraph's next
  action is 6.1's plan.
- The frontier-content validation command below fails on any of: a
  Phase 5 row not marked done, a missing Phase 6 card id, a missing
  exit paragraph, or a stale in-progress/this-PR marker — so the
  receipt cannot certify another stale frontier.

**Retention set (existing, shown unharmed):**

- No file outside the two named docs changes; `plans/**` untouched by
  the task PR.
- The repo-wide gate stays green (`make check`).

## Validation Commands

- `make check-next`
- `make check`
- `sh -c 'p=$(tr "\n" " " < next/docs/progress.md | tr -s " "); for pr in 122 123 124 125 126 131; do echo "$p" | grep -q "merged #$pr" || { echo "PR $pr not recorded merged"; exit 1; }; done; for id in os-f6d2c267 os-6cdc15be os-3128535a os-d2497eb7; do echo "$p" | grep -q "$id" || { echo "$id missing"; exit 1; }; done; echo "$p" | grep -q "Phase 5 exit (the III.F subset" || { echo "exit paragraph missing"; exit 1; }; if echo "$p" | grep -qi "(in progress)"; then echo "stale in-progress marker"; exit 1; fi; if echo "$p" | grep -qi "this PR"; then echo "stale this-PR marker"; exit 1; fi; exit 0'`
