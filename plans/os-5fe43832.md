# Plan: seed docs check holds every relative citation to the tree (os-5fe43832)

**Retrospective plan.** This card landed as a `docs:`-class PR (#305) rather
than a `seed/<id>` task PR, and no plan PR preceded it, so
`plans/os-5fe43832.md` never existed. It was closed with a plain `accept` and
does not carry the D7 no-PR exemption, so this file is the D3
plan-resolvability artifact the done-consistency lint requires. It documents
what was built and how it was verified. It does **not** retroactively confer
plan approval: the authorization of record is the human `accept` already on
the card, and this file exists so the lint can resolve `plans/<task-id>.md`.
It gates no future work.

A relative markdown citation naming a path the tree does not hold renders as a
broken link and nothing caught it. A sweep on 2026-09-04 found 570 relative
links across the repository's markdown and 7 that resolved to nothing, one of
them inside a plan file (os-0dba8c6a's card). The gate already checked
generated-document drift; it did not check that a citation's target exists.

## Steps

1. **Add the citation checker** in `next/internal/docs`: parse every relative
   markdown link in the governed set, resolve it against the citing file's own
   directory, and collect the ones the tree does not hold.
2. **Fold it into `seed docs check`** beside the drift check, with its own
   refusal so a broken citation is distinguishable from a stale rendering.
3. **Drill it** over planted broken and planted valid citations, and fix the
   seven the sweep found.
4. **Record it** in the handbook, the contributor instructions and the
   frontier.

## File Scope

- `next/internal/docs/citations.go` and its test (the checker).
- `next/cmd/seed/docs.go` and `next/cmd/seed/docs_cli_test.go` (the gate).
- `next/internal/envelope/envelope.go`, `next/docs/generated/exit-codes.md`
  (the refusal code and its rendering).
- `next/spec/requests.md`, `docs/CONTRIBUTING-AGENTS.md`,
  `next/docs/handbook.md`, `next/docs/progress.md`, `memory/LEARNINGS.md`
  (the found citations and the record).

## Acceptance Criteria

- `seed docs check` refuses a relative citation naming a path the tree does
  not hold, naming the citing file and the unresolvable target.
- A valid citation, an absolute URL and an anchor-only link all pass.
- The seven citations the sweep found resolve after the change.
- The existing generated-document drift check is unchanged and still green.

## Validation Commands

- `cd next && go test ./internal/docs/... -count=1`: the checker over planted
  broken and planted valid citations.
- `cd next && go test ./cmd/seed -run Docs -count=1`: the gate at the
  terminal, including the refusal's code.
- `cd next && go run ./cmd/seed docs check --root ..`: exits 0 over the real
  tree with an empty `drift` array.
- `git show 1bfea90 --stat`: the eleven files above and nothing else.
- Evidence record: card `os-5fe43832` review block, accepted by `shaunlmason`
  2026-09-07T06:26:09Z, evidence PR #305 merged as `1bfea90`.
