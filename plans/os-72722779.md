# Plan: verify first live CI runs after the v1 branch merges to main (os-72722779)

**Retrospective plan.** This card was executed and accepted directly by a
session (read-only verification of already-run CI) before a plan file
existed. It was closed with a plain `accept` and does not carry the D7
no-PR exemption, so this file is the D3 plan-resolvability artifact that the
done-consistency lint requires. It documents what was authorized and how it
was verified; it is not a gate on any future work.

`check-validate` and `seed-maintenance` execute on `main` only, and every
engine step had already been exercised green locally (build-plan Phase 5).
The card authorized the live confirmation that the workflows actually fire
and pass once `main` exists on GitHub: a PR-triggered `check-validate` run,
a `main`-push `check-validate` run, and the first scheduled
`seed-maintenance` tick.

## Steps

1. **Confirm a PR-triggered `check-validate` run** is green.
2. **Confirm a `main`-push `check-validate` run** is green (and that
   `pr-review` correctly skips in the paths where it is inert).
3. **Confirm the first scheduled `seed-maintenance` tick** is green
   (reap + lint + anchor execute; a merged task auto-closes; anchors mint).

## File Scope

- none — read-only observation of CI runs. No repository file is modified
  by this card; the evidence is the run history itself.

## Acceptance Criteria

- `check-validate` green on the first PR (`32557341065`) and on the first
  `main` push (`32557401822`).
- `pr-review` correctly skipped where inert (`32557341066`).
- First scheduled `seed-maintenance` tick green (`32559730074`, 07:29Z with
  the expected ~12-minute cron lag): reap + lint + anchor executed,
  `os-3e2a8d92` auto-closed from merged PR #5 with green verify, anchors
  `seed-anchor/20260822T072948Z` and `20260822T082906Z` minted; second tick
  `32562393299` green.

## Validation Commands

- `gh api repos/shaunlmason/open-seed/actions/runs --jq '.workflow_runs[]
  | select(.name|test("check-validate|seed-maintenance")) |
  [.name, .conclusion, .head_sha, .html_url]'` — run conclusions per the
  acceptance criteria.
- Evidence record: card `os-72722779` review block (2026-08-22T06:38:48Z and
  08:30:03Z), accepted by `shaunlmason` 2026-08-23T04:18:31Z.
