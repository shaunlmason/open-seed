# Plan: tag template release v0.1.0 (os-62b64ce8)

**Retrospective plan.** This card was executed and accepted directly by a
human (a tag push is a single ref update outside the PR flow) before a plan
file existed. It was closed with a plain `accept` and does not carry the D7
no-PR exemption, so this file is the D3 plan-resolvability artifact that the
done-consistency lint requires. It documents what was authorized and how it
was verified; it is not a gate on any future work.

First published release of the template. `seed template upgrade` resolves
its target by following the `/releases/latest` HTML redirect, which GitHub
serves only once a Release is *published* — so the card covered both the
annotated tag and the published Release, on the `main` HEAD that already
recorded `version = "0.1.0"` in `.seed/template.lock`.

## Steps

1. **Create annotated tag `v0.1.0` on `main` HEAD** (`39b6b64`), with the
   lock file recording `version = "0.1.0"` and the engine pin at
   `v0.13.2`.
2. **Push the tag** to `origin` (`refs/tags/v0.1.0`, peeling to `39b6b64`).
3. **Publish GitHub Release `v0.1.0`** (`isDraft=false`,
   `isPrerelease=false`) so `/releases/latest` resolves.

## File Scope

- none — a tag plus a Release object. No repository file is modified by
  this card; the version record was already committed to
  `.seed/template.lock` by the prior work that produced the tag target.

## Acceptance Criteria

- Tag `v0.1.0` exists on `origin`, annotated, peeling to `39b6b64`.
- Release `v0.1.0` published (not draft, not prerelease);
  `api/releases/latest` resolves `tag_name=v0.1.0`.
- `make check` green at the tagged commit and `check-validate` green on
  `main` at that commit.
- Known gap tracked as follow-up `os-bb20001f`: `seed template upgrade`
  resolves the unauthenticated HTML `/releases/latest` endpoint, which 404s
  while the repository is private — unrelated to the tag itself.

## Validation Commands

- `git ls-remote origin refs/tags/v0.1.0` — tag object present, peels to
  `39b6b64`.
- `gh api repos/shaunlmason/open-seed/releases/latest` — resolves
  `tag_name=v0.1.0`, `isDraft=false`.
- Evidence record: card `os-62b64ce8` review block, `ev-d1e5c8bf`
  (2026-08-23T03:44:33Z), accepted by `shaunlmason` 2026-08-23T04:18:27Z.
