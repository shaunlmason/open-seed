# Plan: next Phase 8.1 — affordance envelope (os-f5551001)

Implements `docs/next-build-plan.md` Phase 8 item 1: the envelope's
`affordances` computed from the same `internal/admit` rule set
admission enforces, with `position`, the budget block, structured
errors, and exit codes per spec. Design authority: SEED-NEXT.md
§II.10 ("the result, plus the verbs currently legal for this actor
on this subject, computed from the same spec and rules admission
enforces — one rule set, two consumers, zero drift by construction …
carry the ledger position they were computed at"), charter III.I
rows 1–2, and the Phase 7 exit, which routes the envelope budget
block here. Much of the shape already exists
(`next/internal/envelope`: version `seed-envelope/0`, structured
`Error`, the exit-code table, position stamping, and the
PLACEHOLDER fields `Affordances []string` — always empty — and
`Budget *Budget` — always nil): this task fills the two
placeholders. Item 2's regression-class harness and item 3's
refusal-rate metric are the phase's next cards; this task lands the
computation and its first pinning drills.

## Design decisions (binding for this task)

- **D1 — affordances are computed by probing the real rule set,
  never by a parallel legality derivation.** The admission matrices
  already draft records and run `admit.Check` on them without
  appending; the affordance computation is exactly that, made a
  library surface: `admit.Affordances(ctx, actor, subject)
  []string` drafts one probe record per catalog verb (the keyring
  spec-table verbs plus the ungoverned active-standing verbs) with
  a minimal payload synthesized from the live context, runs the
  SAME `Check` pipeline, and lists the verb iff the probe admits.
  One rule set, two consumers, zero drift by construction: the only
  new data is the probe-payload table (verb → template filled from
  the context — the active claim fence, an open valid reservation's
  position, the standing pass verdict, a minimal four-part packet
  with the zero-length base range), which is payload SYNTHESIS, not
  legality logic. A verb whose valid payload cannot be synthesized
  from context alone is listed only when its probe admits — honest
  under-listing is acceptable; over-listing is the bug class
  (III.I row 2), which is why the probe runs the enforcing rules
  and not an approximation.
- **D2 — the budget block fills from the one shared derivation.**
  `envelope.Budget{Reserved, Remaining}` populates from
  `admit.BudgetViewAt` at the response's context — `reserved` the
  sum of open valid reservation amounts, `remaining` the derived
  remaining, both decimal strings (7.2's saturating arithmetic) —
  on subjects carrying budget facts; subjects without stay `null`
  (the schema-stable back-compat the spec already promises).
- **D3 — where the CLI stamps them.** Affordances and the budget
  block need ledger + actor + subject. `seed ledger append` stamps
  both on every response (the actor is the signing key's
  fingerprint, the subject the record's; the context is the
  post-append tip, matching the stamped position). `seed budget
  status` gains an optional `--actor <fingerprint>`: with it the
  response stamps affordances beside the budget block it already
  derives; without it, affordances stay `[]` (a read surface must
  not guess an identity). Every other verb keeps `[]`/`null`
  exactly as today — the spec's "subjectless verbs carry an empty
  list" sentence generalizes to "responses lacking a
  ledger+actor+subject context".
- **D4 — refusals stamp affordances too.** A refused append still
  computes affordances for its actor/subject at the refusal's
  context (the tip that refused it): the refusal plus the list of
  what IS legal is the affordance envelope's whole point (a rising
  refusal rate signals an affordance gap; the worker's next legal
  move should be in hand). Computation failures degrade to `[]`,
  never to a failed response: affordances are advisory, and their
  computation must not break the verb.
- **D5 — the drills pin listed-iff-admits on the probe corpus.**
  A library drill walks a fixture through the lifecycle (ready →
  claimed → reserved → started → review → done) asserting at each
  position that (a) every listed verb's probe admits and (b) a
  curated set of known-illegal verbs is absent (claim.taken on an
  in_progress subject, run.started without an open reservation,
  verdict.rendered by a non-verifier, a second run.interrupted on
  the fence); a CLI drill asserts append responses carry the
  stamped affordances and budget block (and `[]`/`null` where D3
  says so). The full property harness — every listed verb refused
  at admission at the same position is a test failure as a CLASS —
  is item 2's card; these drills are its seed corpus.

## Steps

1. **Probe table + computation** (`next/internal/admit`): the verb
   catalog (from the keyring spec rows + ungoverned verbs), the
   probe-payload synthesizers reading the live context/fold, and
   `Affordances(ctx, actor, subject) []string` running the real
   `Check` per probe. Sorted, deduplicated output for schema
   stability.
2. **Budget block helper**: derive `{reserved, remaining}` strings
   from `BudgetViewAt` beside the existing `seed budget status`
   numbers (shared, not duplicated).
3. **CLI stamping** (`next/cmd/seed`): append (success and refusal
   paths) and `budget status --actor` per D3/D4; a small shared
   `stampAffordances(env, store, actor, subject)` that degrades to
   `[]` on computation failure.
4. **Spec**: `next/spec/envelope.md` — the affordances sentence
   gains the D3 context rule and the D4 refusal posture; the
   budget-block sentence drops its "until budgets land" tense and
   documents the two fields' derivation.
5. **Drills**: the D5 library walk in `internal/admit`
   (`affordances_test.go`) and the CLI stamp assertions beside the
   existing append tests; `make check` green with cleaned caches at
   the 90% gate.
6. **Docs**: decision-log entry; `next/docs/progress.md` Phase 8
   ledger opens (8.1 row in review, frontier updated).

## File Scope

- `next/internal/admit/` (new `affordances.go` + `affordances_test.go`)
- `next/internal/envelope/envelope.go` (only if a field comment
  needs updating; the struct already carries both fields)
- `next/cmd/seed/` (append + budget status stamping, CLI drills)
- `next/spec/envelope.md`
- `next/docs/decisions.md`, `next/docs/progress.md`
- `receipts/os-f5551001.json`

## Acceptance Criteria

1. `admit.Affordances` exists, computes by drafting probes through
   the enforcing `Check` pipeline (no parallel legality logic), and
   returns a sorted stable list.
2. `seed ledger append` responses — success and refusal — carry
   affordances for the record's actor/subject at the stamped
   position, and the budget block on budget-active subjects;
   `seed budget status --actor` likewise; every other response is
   byte-identical to today (`[]`/`null`).
3. The D5 drills pass: listed probes admit at the same position,
   the curated illegal set is never listed, and stamping degrades
   to `[]` on computation failure rather than failing the verb.
4. `next/spec/envelope.md` documents the context rule, the refusal
   posture, and the budget derivation.
5. `make check` green from the repo root with cleaned caches;
   coverage at or above the 90% gate.

## Validation Commands

```sh
cd next && gofmt -l . && go vet ./... && go build ./...
cd next && go test ./internal/admit/ ./internal/envelope/ ./cmd/seed/ -run 'Affordance|Envelope|Budget|Append' -count=1
make check
```

## Expected diff shape

Additive: one new admit file pair (computation + drills), two CLI
call sites stamping two already-existing envelope fields, one spec
touch, the two docs files, and the receipt. No schema change (the
fields exist), no new verbs, no v1 surfaces.
