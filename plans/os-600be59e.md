# Plan: next Phase 6 exit — frontier update (os-600be59e)

Records the Phase 6 exit in the frontier file. Design authority:
[`docs/next-build-plan.md`](../docs/next-build-plan.md) Phase 6 *Exit*
(charter III.G minus L2/L3 levels and rubric calibration, deferred to
Phase 10/11 — the reconciliation drills, the receipt
recompute-and-mismatch test, the sealed-check audit) and the
[`AGENTS.md`](../AGENTS.md) rule that `next/docs/progress.md` stays
the accurate frontier fresh agents resume from. Every Phase 6 item is
merged and every Phase 6 card closed (6.1 #135 against plan #133,
6.2 #137 against #136, 6.3 #139 against #138, 6.4 #141 against #140);
the file still shows 6.4 as "this task PR" and the frontier pointing
at landing it — stale exactly where a fresh agent would read it.
Docs-only: no code surface changes. The precedent is the Phase 5 exit
record (plans/os-6e37b10e.md); like it, this administrative card
appears in the exit prose, never as a ledger row.

## Steps

1. **Flip the finished row.** In `next/docs/progress.md`, mark 6.4
   **done** (merged #141; card closed; the review round bound the
   lockout, the return, and the override to boundary-validated fails
   only, revalidated the override's citation at the chain steps, and
   taught the fold's anomaly check the override path), matching the
   wording of the already-done rows.
2. **Record the Phase 6 exit.** One paragraph in the Phase 5
   precedent's scoped-exit form: "Phase 6 exit (the III.G subset
   docs/next-build-plan.md scopes): met." Tie the exit line's three
   named drills to evidence — the induced-divergence reconciliation
   drills (#137: merge_without_verdict, chain_skipped, neutral
   unreconciled, attested_divergence, target_rewritten,
   evidence_missing, verdict_unverified; extended by #139's
   unsealed/seal_unverified and #141's overridden/override_unverified),
   the receipt recompute-and-mismatch test (#135 exit 21, with #139
   putting sealed transcripts inside the recompute boundary), and the
   sealed-check audit (#139: grant disjointness both directions, the
   seal-author claim refusal, the implementer-cannot-decrypt drills,
   recipient-tag audit, rotation without history) — then the III.G
   rows each merged PR evidences: done only through the chain with
   each step its own event (#137, #141's override-backed path
   uncollapsed); verdicts signed by verdict-granted keys provably
   disjoint from implementing keys with operator override its own
   attributable verb, never a disguised verdict (#135 L1 exit 17,
   #141); clean per-run isolation with cleanup firing pass or fail
   and enumerable self-executed inputs (#135); receipts binding
   contract, plan-at-merge-base, diff, inventory, visible and sealed
   transcripts, and environment (#135, #139); a red verdict
   unmergeable and locking the implementer out until a new
   submission (#137's chain-legality half, #141's lockout exit 25
   and contract.returned). Note the Phase 5 exit's sealed-checks
   carve-out as now closed by #139. Close with the unmet III.G
   remainder by landing phase: L2 runtime-tuple separation and L3
   deterministic-first verification per tier (Phase 10's level
   declarations), and rubric verdicts with per-item evidence,
   calibration against a human gold set, and drift-triggered
   authority suspension (Phase 11).
3. **Open the Phase 7 ledger.** Retitle the frontier: list the four
   filed supervisor cards (7.1 os-c61c3392 offers, 7.2 os-cecac5de
   budgets, 7.3 os-1dad487d executor adapter + local worktree
   adapter, 7.4 os-0f718b4e graceful preemption) — one line per
   build-plan item, only those four — and rewrite the Frontier
   paragraph to point at 7.1 (offer.published eligibility-scoped and
   expiring, workers pull and claim, the wakeless poll-only drill
   proving wake is advisory; plan-first) and the Phase 7 exit subset
   (charter III.H for the implemented adapter: poll-only run,
   reservation race drill, disposability drill).
4. **Durable insight.** Append to `memory/LEARNINGS.md`: the
   laundering countermeasure now has a fixed shape worth reusing on
   sight — any admitted step that trusts a folded fact must (a)
   validate the fact's signer against its authoring boundary and (b)
   revalidate the fact's own citations, because the tolerant fold
   records any well-shaped raw push; 6.2 learned it for verdicts,
   6.3 for seals, 6.4 for fails and overrides, and Phase 7's offers
   and budget reservations will meet it again.

## File Scope

- `next/docs/progress.md`
- `memory/LEARNINGS.md`

## Acceptance Criteria

**Boundary set (new, shown working):**

- The 6.4 row reads done (merged #141; card closed) in the standard
  form; no Phase 6 row carries an in-progress marker.
- The exit paragraph exists, scoped exactly as the build plan's exit
  line ("the III.G subset docs/next-build-plan.md scopes: met"),
  cites the three named exit drills with their PRs, and routes the
  unmet remainder (L2/L3 levels, rubric calibration) to Phase 10/11
  by name.
- The Phase 7 ledger block lists exactly the four filed cards with
  their ids, and the Frontier paragraph points at 7.1's plan as the
  next action with the Phase 7 exit subset named.
- The frontier survives the anchored staleness sweep: every Phase 6
  task PR row (135, 137, 139, 141) matches the done-with-card-closed
  anchor; the four Phase 7 card ids are present; the exit-paragraph
  anchor matches; no case-insensitive stale marker ("(in progress)",
  "this PR", "pending") remains.

**Retention set (existing, shown unharmed):**

- `make check` (which runs `check-next`) is green; no code, spec,
  receipt, or `.seed/**` changes; earlier phase records (the Phase 5
  exit paragraph included) are untouched except where a stale marker
  had to die.
- No v1 surface changes; the task PR never touches `plans/**`.

## Validation Commands

- Boundary: `make check-next`
- Boundary: `sh -c 'p=$(tr "\n" " " < next/docs/progress.md | tr -s " "); for pr in 135 137 139 141; do echo "$p" | grep -qF "done** (merged #$pr; card closed" || { echo "PR $pr row not done with card closed"; exit 1; }; done; for id in os-c61c3392 os-cecac5de os-1dad487d os-0f718b4e; do echo "$p" | grep -q "$id" || { echo "$id missing"; exit 1; }; done; echo "$p" | grep -q "Phase 6 exit (the III.G subset" || { echo "exit paragraph missing"; exit 1; }; for bad in "(in progress)" "this PR" "pending"; do if echo "$p" | grep -qi "$bad"; then echo "stale marker: $bad"; exit 1; fi; done; exit 0'`
- Retention: `make check`

## Expected diff shape

Edits to two documentation files only: one reworded ledger row, one
exit paragraph, a rewritten frontier with the Phase 7 ledger block,
and one appended learning. No code, spec, receipt, or `.seed/**`
changes; no `plans/**` in the task PR.
