/**
 * Minimal scaffolding so a freshly flavored repo is green out of the box.
 * A flavor that instantiates red teaches agents to ignore `make check`.
 */
export const greet = (name: string): string => `hello, ${name}`;
